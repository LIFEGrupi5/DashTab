apiVersion: v2
name: rabbitmq
description: DashTab RabbitMQ — message broker (Bitnami wrapper)
type: application
version: 0.1.0
appVersion: "3"

dependencies:
  - name: rabbitmq
    version: "15.x.x"
    repository: oci://registry-1.docker.io/bitnamicharts
