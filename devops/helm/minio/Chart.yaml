apiVersion: v2
name: minio
description: DashTab MinIO — S3-compatible object storage (Bitnami wrapper)
type: application
version: 0.1.0
appVersion: "latest"

dependencies:
  - name: minio
    version: "14.x.x"
    repository: oci://registry-1.docker.io/bitnamicharts
