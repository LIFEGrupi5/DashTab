apiVersion: v1
kind: Service
metadata:
  name: dashtab-prometheus
  labels:
    app.kubernetes.io/name: prometheus
    app.kubernetes.io/part-of: dashtab
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: prometheus
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
