apiVersion: v2
name: redis
description: DashTab Redis — cache + SignalR backplane (Bitnami wrapper)
type: application
version: 0.1.0
appVersion: "7"

dependencies:
  - name: redis
    version: "20.x.x"
    repository: oci://registry-1.docker.io/bitnamicharts
