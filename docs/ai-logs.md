# AI Development Log

## Summary Statistics
- Total entries: **104** (Milestone 1 table: 4 rows; Milestone 2 / session chronicle: **#1–#36**; post–M2.7 Cursor session: **#37–#40**; Apr 19 polish session: **#41–#49**; Apr 24 Docker + frontend fixes: **#50–#55**; Apr 28 perf + FE requirements session: **#56–#64**; May 6 dev environment + architecture + API wiring: **#65–#72**; May 6 FE-2 / FE-9 requirements pass: **#73–#76**; May 7 M3.13 Serilog + correlation IDs: **#77**; Apr 25 backend Clean Architecture scaffold: **#78–#79**; May 4–5 backend entities + Swagger + exception handler: **#80–#83**; May 6 backend DB layer: **#84–#85**; May 6 Keycloak + RBAC + validation + rate limiting: **#86–#88**; May 9 S3-17 frontend Keycloak wiring: **#89**; May 8 Redis cache + Keycloak auth debugging: **#90–#94**; May 11 Hangfire + email confirmation: **#95–#96**; May 10 M3.4 SQL optimization indexes + EXPLAIN ANALYZE doc: **#97–#98**; May 11 MinIO object storage integration: **#99–#101**; May 12 MinIO hardening + backend hygiene: **#102–#104**)
- Estimated total time saved: ~84h
- Most used tool: Claude / Cursor Agent
- Updated through: Milestone 2 + M2.7 a11y/Lighthouse + **Apr 16–17** git-dated sessions + **Apr 18, 2026** kitchen board & dashboard shell (Cursor) + **Apr 19, 2026** UI polish & kitchen board UX (Claude Code) + **Apr 24, 2026** Docker bring-up & frontend fixes (Claude Code) + **Apr 28, 2026** performance audit + FE requirements pass (Claude Code) + **Apr 25, 2026** backend Clean Architecture scaffold (Claude Code) + **May 4–5, 2026** backend entities, endpoints, Swagger, exception handler (Claude Code) + **May 6, 2026** dev environment hardening + architecture review + frontend/backend wiring (Claude Code) + **May 6, 2026** FE §3.2 audit + FE-2 / FE-9 follow-up (Claude Code) + **May 6, 2026** backend DB layer — soft delete, audit trail, migrations (Claude Code) + **May 6, 2026** Keycloak JWT auth + RBAC + FluentValidation + rate limiting (Claude Code) + **May 7, 2026** M3.13 Serilog + correlation IDs (Claude Code) + **May 9, 2026** S3-17 frontend Keycloak wiring (Claude Code) + **May 8, 2026** Phase 3A Redis cache-aside + Keycloak auth debugging (Claude Code) + **May 11, 2026** S3-25/S3-26 Hangfire + transactional email (Claude Code) + **May 10, 2026** M3.4 SQL optimization — indexes + EXPLAIN ANALYZE doc (Claude Code) + **May 11, 2026** MinIO object storage integration (Claude Code) + **May 12, 2026** MinIO hardening + backend hygiene (Claude Code)

---

## Milestone 1 — Product Foundation (Due Mar 27)

### Enes    

| # | Tool | Area | Purpose | Output Quality | Time Saved | Lessons Learned |
|---|---|---|---|---|---|---|
| 1 | Claude | PM | Project brief analysis — risks and grading traps | Good | ~1h | Give Claude the full document, it catches non-obvious requirements |
| 2 | Claude | PM | README review and rewrite | Good | ~45min | Ask what's wrong first, then ask for a rewrite |
| 3 | Claude | PM | CLAUDE.md files for monorepo | Needed 2-3 rounds | ~1.5h | Specify ports, current state, and length upfront |
| 4 | Claude | PM | AI log structure for team project | Good | ~20min | Ask about team-specific concerns early |



### [Name 2]
| # | Tool | Area | Purpose | Prompt Summary | Output Quality | Time Saved | Lessons Learned |
|---|---|---|---|---|---|---|---|




### [Name 3]
| # | Tool | Area | Purpose | Prompt Summary | Output Quality | Time Saved | Lessons Learned |
|---|---|---|---|---|---|---|---|

### Milestone 1 Reflection
What worked as a team, what didn't, what we'd do differently.

---

## Milestone 2 — Frontend MVP (Due Apr 17)


| # | Tool | Area | Purpose | Output Quality | Time Saved | Lessons Learned |
|---|---|---|---|---|---|---|

| 1 | Claude | Frontend | Milestone audit M2.4–M2.9 — checked what exists, what's missing, and what's blocked | Good | ~30min | Spawning an Explore agent against the full frontend gave a complete picture faster than manual grep |
| 2 | Claude | Frontend | M2.8 — bundle analyzer setup (`@next/bundle-analyzer`, `ANALYZE=true` script) | Good | ~20min | One config file change, immediately useful for future dep audits |
| 3 | Claude | Frontend | M2.8 — Web Vitals tracking component wired into root layout | Good | ~30min | Using dynamic `import('web-vitals')` in a `useEffect` avoids adding to the critical JS path |
| 4 | Claude | Frontend | M2.8 — code splitting: extracted `KitchenBoard` into dynamically-imported client component with loading skeleton | Needed one fix round | ~45min | Next.js 15 blocks `ssr: false` inside Server Components — requires a thin `'use client'` wrapper; this is the correct enterprise pattern |
| 5 | Claude | Frontend | M2.4 — Zod schema for staff form (`lib/schemas.ts`) with enum validation for roles and required field rules | Good | ~15min | Keep schemas simple and flat — one schema per form avoids type complexity with React Hook Form |
| 6 | Claude | Frontend | M2.4 — `FileUpload.tsx` with click-to-browse, image-only filter, 5MB limit | Good | ~20min | Keep file state in the parent via `useState` so it can be included in form submission |
| 7 | Claude | Frontend | M2.4 — `MultiStepForm.tsx` 3-step wizard with per-step validation via `trigger()` | Needed 2 fix rounds | ~1h | Don't wrap multi-step forms in a `<form>` tag — it causes accidental submission on intermediate steps; use a `<div>` and call `trigger()` + `getValues()` manually |
| 8 | Claude | Frontend | M2.4 — bug fix: role validation failing despite dropdown showing a value | Good | ~10min | Always set `defaultValues` for every field including dropdowns — without it React Hook Form sees `undefined` and Zod enum check fails |
| 9 | Claude | DevOps | CI pipeline architecture advice — asked Claude where workflows should live in a monorepo and how to split them per service | Good | ~45min | GitHub Actions workflows must live in `.github/workflows/` regardless of monorepo structure; `devops/ci/` is for scripts, not workflow YAMLs |
| 10 | Claude | DevOps | Frontend CI jobs — asked Claude to review and extend the pipeline with unit test, e2e, and Lighthouse jobs | Good | ~30min | Separate jobs run in parallel so a lint failure doesn't hide test failures |
| 11 | Claude | Frontend | M2.6 — asked Claude to explain and set up Jest + RTL and Playwright, then fixed errors that came up during configuration | Needed fixes | ~1h | `setupFilesAfterEnv` not `setupFilesAfterFramework`; co-locating tests in `src/frontend/tests/` avoids tsconfig path issues that arise when tests live outside the project root |
| 12 | Claude | DevOps | M2.8 — asked Claude to explain Lighthouse and wire up `@lhci/cli` into CI with thresholds | Good | ~20min | Set accessibility threshold to `error` immediately; performance can be `warn` until baseline is established |

### Past sessions (Apr 2026) — integration, theming, and data

Follow-up work after the M2 baseline: re-wired the client stack (TanStack Query + Zustand + mock API), aligned auth and pages, completed dark mode on remaining surfaces, fixed mobile sidebar behavior, and adjusted mock menu pricing. Entries below continue the Milestone 2 table.

| # | Tool | Area | Purpose | Output Quality | Time Saved | Lessons Learned |
|---|---|---|---|---|---|---|
| 13 | Cursor Agent | Frontend | Restored data layer: `@tanstack/react-query` + DevTools, Zustand store with `persist`/`devtools`, `lib/queryKeys.ts`, `lib/api/mock.ts`, hooks (`useOrders`, `useMenu`, `useUsers`, `useAuth`, `useStoreHydrated`), `AppProviders` (`QueryClientProvider`, dark-class sync, auth query warmup) wired in root layout | Good | ~2h | Keep query keys centralized; `suppressHydrationWarning` on `<html>` avoids theme flash; validate `package.json` after merges so `zod` and `zustand` both stay listed |
| 14 | Cursor Agent | Frontend | Auth flow: login calls `setAuth` + `mockTokenForUserId`, `queryClient.invalidateQueries({ queryKey: ['auth'] })`; `clearAuth` on sign-out; role/nav driven from store instead of only `localStorage` | Good | ~25min | Legacy `localStorage` keys can stay cleared in `clearAuth` until all readers are migrated |
| 15 | Cursor Agent | Frontend | Theming: softer `.dark` CSS variables (`globals.css`); `Button`, `PageHeader`, `TextField`, `Modal`, `StatCard` use `dark:` + `bg-card` / `border-border` patterns | Good | ~45min | Prefer design tokens over ad-hoc grays so charts and modals stay consistent |
| 16 | Cursor Agent | Frontend | Dashboard shell: role-based nav (waiter vs owner), sidebar expand/collapse + dark mode toggle, hydration gate for layout; pages (orders, dashboard, menu, staff, `orders/new`) wired to hooks and mock data | Good | ~1.5h | `useStoreHydrated` prevents wrong nav or redirects before Zustand rehydrates; single mock source reduces drift between list and detail views |
| 17 | Cursor Agent | Frontend | Dark mode sweep: `KitchenBoard` + `DynamicKitchenBoard` loading skeleton, Overview “Orders by Hour” / “Top Ordered Items”, `MultiStepForm`, signup page, `FileUpload` | Good | ~45min | Inner chart/track areas often need their own `dark:bg-*` strip, not only the outer card |
| 18 | Cursor Agent | Frontend | Kitchen kanban: semantic `dark:` on columns and order cards; added primary action on Ready column (“Mark Ready”) to match other columns | OK | ~10min | Label may later change to “Complete” / “Served” once the real workflow is fixed |
| 19 | Cursor Agent | Frontend | Mobile sidebar: fixed `w-14` icon rail below `sm`; `sm+` uses stored open/closed width; tightened padding, `shrink-0`, `overflow-x-hidden` on shell; hide panel toggle on xs where width does not change | Good | ~35min | Nav labels stay `hidden sm:inline` — pairing with a narrow fixed rail avoids a wide empty sidebar on phones |
| 20 | Cursor Agent | Frontend | Mock data: Byrek menu price set to **€1.20**; order #003 line amounts and `totalAmount` recalculated; Overview top-items Byrek revenue updated (6 × €1.20) | Good | ~10min | After any price change, grep the dish name and update order line totals, not only `MOCK_MENU` |

### Session — Apr 16, 2026 (yesterday vs. Apr 17 push; from `git log`)

Work landed the **day before** the large Apr 17 merge day: waiter-focused UI, repo docs, and project/backend foundation. These sessions complement the “Apr 2026 integration” block above (some ideas were iterated again when TanStack/Zustand landed).

| # | Tool | Area | Purpose | Output Quality | Time Saved | Lessons Learned |
|---|---|---|---|---|---|---|
| 21 | Cursor / team | Frontend | **Waiter experience (PR #5 / `design-waiter`):** waiter-only dashboard & orders flows, role-based navigation (fewer routes for waiter), layout tuned for floor staff | Good | ~1.5h | Role from `localStorage` was fine for demos; migrating to Zustand + query cache (later entries) removed split-brain reads |
| 22 | Cursor / team | Docs | **`ai-logs.md` + CLAUDE.md:** team added root / backend / frontend / devops CLAUDE files and the shared AI development log for coursework handoff | Good | ~45min | Same themes as M1 rows 3–4, but as committed repo artifacts — worth one pointer in the log so graders see the timeline |
| 23 | Cursor / team | DevOps / monorepo | **Project setup + backend API merge:** structure and backend scaffold brought into `main` so frontend features had a stable base | Good | ~1h | Backend path clarified early; frontend CI and app work could proceed in parallel branches |
| 24 | Cursor / team | Frontend | **Design polish on waiter path:** typography, order filters / tabs, spacing — aligned with “operations” feel before dark-mode and sidebar refactors | Good | ~45min | Filter/toolbars that wrap cleanly on small widths paid off when the mobile sidebar rail shipped later |

### Session — Apr 17, 2026 (next day: tests, CI, state, dark mode — from `git log`)

Heavy integration day (merges, CI fixes, M2.5 state branch, dark-mode/responsive sidebar). Logged here so “yesterday” (Apr 16) vs. “integration day” (Apr 17) stay distinguishable in the submission.

| # | Tool | Area | Purpose | Output Quality | Time Saved | Lessons Learned |
|---|---|---|---|---|---|---|
| 25 | Cursor / team | Frontend | **M2.6 + M2.8:** Jest/RTL, Playwright e2e, Lighthouse CI, GitHub Actions — first full pipeline | Needed fixes | ~2h | `setupFilesAfterEnv` naming; Lighthouse thresholds relaxed then tightened in follow-up commits |
| 26 | Cursor / team | Frontend | **M2.4 forms:** staff Zod schema, `MultiStepForm`, `FileUpload` — wizard validation with `trigger()` / `getValues()` | Good | ~1.5h | Matches M2 table rows 5–8; session work was finishing PR and fixing submission edge cases |
| 27 | Cursor / team | DevOps | **CI hardening:** ESLint CLI migration, Lighthouse CI assertion tweaks, Jest-axe pass, pipeline fixes (`fixing ci pipeline`, lint-lighthouse PRs) | Needed fixes | ~1.5h | Parallel jobs help, but flaky e2e/Lighthouse need retries or `continue-on-error` only where documented |
| 28 | Cursor / team | Frontend | **`dark-mode-fix` / responsive sidebar:** `fix(frontend): dark-mode classes across pages and responsive sidebar` + merge with `development` / `m2.5-state-setup` | Good | ~1.5h | Overlaps entries 15–19 — Apr 17 commit is the branch that landed in repo; keep one line here for git traceability |
| 29 | Cursor / team | Tooling | **`npm installed` / dependency sync** after merges — lockfile aligned for CI reproducibility | Good | ~15min | After big merges, run `npm ci` locally and in CI to catch lockfile drift early |
| 30 | Cursor / team | Frontend | **M2.5 state setup (WIP → merge):** TanStack Query + Zustand direction merged with `development` (precursor to entries 13–16) | Good | ~2h | Feature branch needed explicit merge resolution; `Merged with main` commits mark integration checkpoints |

### Session — M2.7 accessibility + M2.8 Lighthouse run

| # | Tool | Area | Purpose | Output Quality | Time Saved | Lessons Learned |
|---|---|---|---|---|---|---|
| 31 | Claude | Frontend | M2.8 — ran `npm run build && npx lhci autorun`, generated Lighthouse reports for `/login` and `/dashboard`, saved results in `.lighthouseci/` | Good | ~20min | Build must succeed before `lhci autorun` — `npm run start` needs a compiled app; run `npm install` after pulling to avoid missing package errors |
| 32 | Claude | Frontend | M2.7 — identified 2 real violations from Lighthouse output: `color-contrast` on login badge (`text-orange-600 bg-orange-50`, ratio ~2.3:1) and `link-name` on dashboard nav (icon-only links have no text for screen readers) | Good | ~15min | Read the Lighthouse output first before writing any code — it tells you exactly what to fix |
| 33 | Claude | Frontend | M2.7 — fixed `color-contrast`: login badge changed from `text-orange-600 bg-orange-50` → `text-orange-800 bg-orange-100` (ratio ~4.8:1, passes WCAG AA) | Good | ~5min | WCAG AA requires 4.5:1 for small text — orange-800/100 is the correct Tailwind pairing |
| 34 | Claude | Frontend | M2.7 — fixed `link-name`: added `aria-label={item.label}` to every nav link in `layout.tsx` so screen readers can read icon-only links on mobile | Good | ~5min | Always pair icon-only interactive elements with `aria-label` — it is invisible on screen but read aloud by screen readers |
| 35 | Claude | Frontend | M2.7 — wrote `accessibility.test.tsx` using `jest-axe` to automatically catch violations on `Button` and `Modal` in every test run | Good | ~15min | `expect.extend(toHaveNoViolations)` must be called at the top of the file, not inside a `beforeAll` |
| 36 | Claude | Frontend | M2.7 — fixed CI failure: `jest-axe` was installed locally but not saved to `package.json`; CI runs `npm install` from `package.json` so it could never find the package | Good | ~10min | Always use `npm install -D` — the `-D` flag writes the package to `devDependencies` in `package.json`; without it only your machine has it |

### Session — Apr 18, 2026 (kitchen board + dashboard shell; Cursor)

| # | Tool | Area | Purpose | Output Quality | Time Saved | Lessons Learned |
|---|---|---|---|---|---|---|
| 37 | Cursor Agent | Frontend | **Kitchen board:** `KitchenBoard` cards + timing (`placedAtIso` / `stageEnteredAtIso`), `enrichOrderKitchenTimes` + `ensureOrderKitchenTimes` in `mock.ts`, `useOrders` `select` for stale shapes, `useSetOrderStatus`, `DynamicKitchenBoard` skeleton, relative “ago” + `HH:mm` | Good | ~1h | Keep `Order` fields aligned with API; drop demo enrich when backend returns real timestamps |
| 38 | Cursor Agent | Frontend | **Layout / roles:** `kitchenNavItems` + sidebar profile for kitchen; login redirect `kitchen` → `/kitchen`; active state for `/kitchen` | Good | ~25min | KDS users land on board; shell nav stays minimal for that role |
| 39 | Cursor Agent | Frontend | **Sidebar profile:** owner & manager get same avatar + name + role block as waiter/kitchen; “Log Out” label aligned; avatar styling tuned | Good | ~15min | One `roleShowsSidebarProfile` helper avoids drift between footer spacing and label |
| 40 | Cursor Agent | Docs | **`ai-logs.md`:** summary counts reconciled with numbered tables (#1–#36 + this block); time-saved estimate nudged | Good | ~5min | When tables add rows, bump “Total entries” in the same commit so graders stay aligned |

### Session — Apr 19, 2026 (UI polish & kitchen board UX; Claude Code)

| # | Tool | Area | Purpose | Output Quality | Time Saved | Lessons Learned |
|---|---|---|---|---|---|---|
| 41 | Claude Code | Frontend | Removed signup page: stripped “Sign up” link and unused `Next/link` import from `login/page.tsx`, deleted `app/(auth)/signup/` directory | Good | ~5min | Remove unused imports immediately after removing the JSX that references them to keep the build clean |
| 42 | Claude Code | Frontend | Kitchen board navbar responsiveness: added `mx-auto` to header inner container and content area (`max-w-6xl` was left-anchored, looked off-center) | Good | ~10min | A `max-w-*` container without `mx-auto` is silently left-aligned — always pair them |
| 43 | Claude Code | Frontend | Click-outside-to-close on all modals: added `onClick={onClose}` on the backdrop div and `onClick={e => e.stopPropagation()}` on the inner panel for both `Modal.tsx` and `MultiStepForm.tsx` | Good | ~10min | `stopPropagation` on the inner panel is required — without it any click inside the modal bubbles up and closes it |
| 44 | Claude Code | Frontend | Page layout: replaced `max-w-*xl mx-auto` fixed-pixel constraints on dashboard, orders, menu, staff, overview, and kitchen board with `w-[95%] mx-auto` so content scales proportionally with screen width | Good | ~15min | Percentage width feels more “app-like” on large monitors; fixed `max-w` leaves obvious empty side bands above ~1400px |
| 45 | Claude Code | Frontend | Kitchen board layout: changed from vertically stacked status sections to a 3-column Kanban grid (`grid-cols-3`) with cards flowing downward within each column; updated `KITCHEN_CARD_WRAP` to `w-full` | Good | ~15min | Kanban columns are far more scannable than horizontal card rows that wrap unpredictably |
| 46 | Claude Code | Frontend | Sidebar collapsed-state symmetry: logo row and button row were misaligned when collapsed (logo had `px-3` left-bias, buttons were `justify-end`); conditionally applied `justify-center` on both rows when `!sidebarOpen` | Good | ~10min | Collapsed icon rails should center everything — expanded state can use `justify-between`; drive both from the same `sidebarOpen` flag |
| 47 | Claude Code | Frontend | Sidebar nav icons: increased from `w-4 h-4` to `w-5 h-5` for better visibility and touch target size | Good | ~2min | Icon size in a sidebar should match the surrounding padding rhythm — `w-5` fits `px-3 py-2.5` better than `w-4` |
| 48 | Claude Code | Frontend | Kitchen board card click-to-advance: made the entire order card div clickable (`onClick={onAdvance}`, `cursor-pointer`, subtle `hover:brightness` + `active:scale`) instead of only a button at the bottom; removed the separate action button | Good | ~10min | Touch-friendly KDS boards work best when the whole card is the tap target — small buttons are hard to hit under pressure |
| 49 | Claude Code | Frontend | Kitchen board interactivity debugging: confirmed click-to-advance only works when logged in as kitchen staff (`petrit@restaurant.com`) — `interactive={isKitchenStaff}` gates the `onClick`; tested by temporarily opening to all roles then reverting | Good | ~5min | When a feature appears broken, check role guards before assuming a UI bug — the logic was correct, just the test account was wrong |

### Session — Apr 24, 2026 (Docker bring-up + frontend fixes; Claude Code)

| # | Tool | Area | Purpose | Output Quality | Time Saved | Lessons Learned |
|---|---|---|---|---|---|---|
| 50 | Claude Code | DevOps | Stack wasn't running — went through the Docker and Compose files together to catch issues before starting; Claude spotted the missing Keycloak DB owner and a few config gaps | Good | ~30min | Always read configs before running; small issues like missing DB ownership compound into confusing runtime failures |
| 51 | Claude Code | Backend | Hit a compile error and a silent routing bug during Docker build — Claude helped identify that `WeatherForecast.cs` was missing and that `UseHttpsRedirection` breaks inside an HTTP-only container | Good | ~20min | In Docker, TLS belongs at the nginx layer not inside the app; gate `UseHttpsRedirection` behind `IsDevelopment()` |
| 52 | Claude Code | DevOps | Ran into port conflicts and a stubborn Keycloak `unhealthy` status — Claude helped trace each one; ports killed manually, Keycloak needed three separate env var fixes | Needed several rounds | ~45min | Debug healthchecks by running the command directly inside the container with `docker exec` — it tells you immediately if the service or the check is broken |
| 53 | Claude Code | DevOps | nginx kept crashing with `host not found in upstream` — Claude explained why and how to fix the routing without breaking proxy paths | Good | ~20min | nginx resolves upstreams at startup; a `set $upstream` variable defers DNS to request time; can't concatenate a path after a variable — use `rewrite` to strip prefixes instead |
| 54 | Claude Code | Frontend | Asked Claude to review and score the frontend — wanted a clear picture of what was solid and what was broken before deciding what to fix | Good | ~20min | An outside read caught broken order creation, missing toasts, modal a11y gaps, and no error states — easy to miss from inside the codebase |
| 55 | Claude Code | Frontend | Worked through the priority fixes together — modal accessibility, wiring up `sonner`, making order creation functional with cart state, and connecting the status buttons | Good | ~45min | Derive values from state rather than storing them separately; reset modal state on close not on open |

### Session — Apr 28, 2026 (performance audit + FE requirements pass; Claude Code)

Full session focused on two tracks: closing gaps in the FE requirements checklist (FE-2, FE-7, FE-8, FE-12) and resolving the root cause of laggy compilation and runtime performance issues.

| # | Tool | Area | Purpose | Output Quality | Time Saved | Lessons Learned |
|---|---|---|---|---|---|---|
| 56 | Claude Code | Frontend | **Bundle audit:** identified `recharts` as the real cause of 2 700+ module compile count — it was imported synchronously at the page level; also added `experimental.optimizePackageImports` to `next.config.ts` for recharts, lucide-react, and all 10 `@radix-ui` packages | Good | ~30min | `optimizePackageImports` must go under `experimental` in Next.js 15 — as a top-level key it throws "Unrecognized key" silently then is ignored |
| 57 | Claude Code | Frontend | **FE-12 Framer Motion:** added `<motion.div key={pathname}>` page-enter transition in dashboard layout (`opacity 0→1, y 6→0, 180ms`); extracted all recharts + RadixTooltip into `OverviewCharts.tsx` loaded via `dynamic()` with `ssr: false` — `/overview` First Load JS dropped from 250 kB to 116 kB (−53%) | Good | ~45min | `dynamic()` with `ssr: false` is the right boundary for any chart library; the loading skeleton must match the real content height to avoid layout shift |
| 58 | Claude Code | Frontend | **FE-2 useReducer:** built `hooks/useOrderModal.ts` — `ModalState` + `ModalAction` discriminated union (`OPEN / CLOSE / SET_TABLE / ADD_ITEM / REMOVE_ITEM`); replaced 3 `useState` calls in the orders page with a single `useReducer`; all dispatchers wrapped in `useCallback` | Good | ~30min | Discriminated union actions make the reducer exhaustive and self-documenting; `CLOSE` resets to `initialState` so stale cart data can never leak into a new session |
| 59 | Claude Code | Frontend | **FE-8 Accessibility:** added `useId()` + `htmlFor` association in `TextField`; `focus-visible:ring-2 ring-orange-500` on `Button`; ARIA tab pattern on orders page (role tablist/tab, `aria-selected`, tabIndex roving, Arrow/Home/End keyboard nav via `useTabKeyboard` hook) | Good | ~45min | `useId()` generates stable SSR-safe IDs — never use `Math.random()` for label associations; roving tabIndex requires resetting all non-selected tabs to `−1` |
| 60 | Claude Code | Frontend | **FE-7 Tests:** created 6 test files — `TextField.test.tsx` (5 tests), `StatCard.test.tsx` (3), `PageHeader.test.tsx` (5), `StatusBadge.test.tsx` (4), `useOrderModal.test.ts` (7), `accessibility.test.tsx` expanded to 5 axe checks; plus `navigation.spec.ts` (5 Playwright e2e tests) | Good | ~1h | `getByLabelText` only works after label/input are associated via `htmlFor` + `id` — fixing the a11y issue in entry 59 was a prerequisite for the TextField test |
| 61 | Claude Code | Frontend | **ReactQueryDevtools lazy:** changed static import in `AppProviders.tsx` to `dynamic(() => import(...), { ssr: false })` — removes devtools from the production bundle entirely | Good | ~10min | Devtools are a `devDependencies` package but Next.js still bundles them unless the import is deferred; dynamic import is the correct pattern |
| 62 | Claude Code | Frontend | **Performance audit:** traced four runtime lag sources — `KitchenOrderTiming` had a per-card 1s `setInterval` (10+ renders/sec with 10 orders), `IsometricLauncher` fired `setState` on every raw `mousemove` event (60 Hz), `modalTotal` recalculated on every parent render without memoization, `window.location.reload()` used for error retry | Good | ~20min | Profile re-render count before optimizing — the per-card interval pattern is invisible in code review but immediately visible in React DevTools Profiler |
| 63 | Claude Code | Frontend | **KitchenBoard timer consolidation:** lifted the 1-second tick from `KitchenOrderTiming` and the 30-second SLA tick from `KitchenOrderCard` into a single `setInterval(30_000)` in the `KitchenBoard` parent; `now` passed as a prop — reduces from N timers to 1 | Good | ~15min | The "ago" label only changes by the minute so 30-second granularity is accurate enough; killing N−1 intervals is the correct fix, not batching state |
| 64 | Claude Code | Frontend | **Remaining perf fixes:** rAF throttle on `IsometricLauncher` `onMouseMove` (caps state updates to one per animation frame); `modalTotal` wrapped in `useMemo([modal.state.cart, menuItems])`; Retry button replaced `window.location.reload()` with `refetch()` from TanStack Query; `Modal.tsx` `onClose` ref-pinned so the `keydown` effect never re-attaches on parent re-renders | Good | ~20min | `requestAnimationFrame` deduplication is the standard throttle for mouse handlers — `Date.now()`-based throttles still fire unnecessarily; ref-pinning a callback is always the right pattern when an effect must have `[]` deps but needs to call a fresh version of a prop |

---

### Session — May 6, 2026 (dev environment hardening + architecture review + frontend/backend wiring; Claude Code)

Full session covering three areas: production-quality local dev environment, architectural gap analysis and fixes, and full frontend-to-backend API wiring replacing all mock data.

| # | Tool | Area | Purpose | Output Quality | Time Saved | Lessons Learned |
|---|---|---|---|---|---|---|
| 65 | Claude Code | DevOps | **Docker compose hardening:** added rabbitmq:3-management-alpine, minio, mailhog, promtail to docker-compose; refactored all hardcoded secrets and ports to `${VAR}` env references; created `.env.example` with dev-safe defaults; added promtail config for container log shipping to Loki; all 12 services boot clean | Good | ~45min | `.env.example` is documentation — it tells teammates what vars to set without committing real secrets; docker compose reads `.env` automatically from the same directory |
| 66 | Claude Code | Architecture | **Full codebase audit:** reviewed backend Clean Architecture layer purity (Domain, Application, Infrastructure, API), EF Core setup, frontend structure, CI pipelines, and test coverage; produced prioritised list of real issues vs acceptable stubs | Good | ~30min | Honest architecture review catches three things that code review misses: layer purity violations, half-wired features that look complete, and missing input validation at API boundaries |
| 67 | Claude Code | Backend | **Security fix — hardcoded DB password:** removed `ConnectionStrings:Default` (with plaintext password) from committed `appsettings.Development.json`; added `UserSecretsId` to `DashTab.API.csproj`; moved connection string to .NET user secrets (machine-local, never in git); updated `CLAUDE.md` with setup instructions for teammates | Good | ~15min | `dotnet user-secrets` stores secrets in `%APPDATA%/Microsoft/UserSecrets/` — git never sees this path; teammates run one `dotnet user-secrets set` command after clone |
| 68 | Claude Code | Backend | **FluentValidation:** added validators for all request DTOs — `CreateOrderRequest` (table required, items non-empty), `CreateOrderItemRequest` (qty 1–100), `UpdateOrderStatusRequest` (valid enum value), `CreateMenuItemRequest`/`UpdateMenuItemRequest` (price > 0 and ≤ 10 000), `CreateCategoryRequest`, `LoginRequest` (email format), `CreateStaffRequest`/`UpdateStaffRequest` (email format, valid role); wired `AddFluentValidationAutoValidation()` + `AddValidatorsFromAssemblyContaining` in `Program.cs` | Good | ~30min | `AddFluentValidationAutoValidation` is an extension on `IServiceCollection` from `FluentValidation.AspNetCore` namespace — requires explicit `using FluentValidation.AspNetCore;` even with implicit usings enabled |
| 69 | Claude Code | Frontend | **API layer:** created `lib/api/types.ts` (all shared types extracted from mock.ts), `lib/api/client.ts` (typed `fetch` wrapper with auth headers, 401→clearAuth, `ApiError` class), `lib/api/orders.ts` / `menu.ts` / `staff.ts` / `auth.ts` (one function per endpoint); created `lib/orders/kitchenTimes.ts` for kitchen stage timer helpers; added `.env.local` + `.env.local.example` for API base URL | Good | ~45min | Read token via `useAppStore.getState().token` (not a hook) inside the client so it works outside React; `NEXT_PUBLIC_` prefix is required for Next.js to expose env vars to the browser bundle |
| 70 | Claude Code | Frontend | **Read hooks wired to real API:** rewired `useOrders`, `useMenu`, `useUsers`, `useAuth` to call real endpoints instead of mock functions; set `staleTime: 10_000` + `refetchInterval: 30_000` on orders for kitchen board freshness | Good | ~15min | `staleTime: 1000 * 60 * 60 * 24` (24h) was appropriate for mock data but breaks real data — background refetch never fired; 10s stale + 30s poll is the right balance for a kitchen display |
| 71 | Claude Code | Frontend | **Mutation hooks + caller updates:** converted `useCreateOrder` and `useSetOrderStatus` from synchronous cache-only operations to real `useMutation` calls with query invalidation and toast feedback; updated all caller sites — `orders/page.tsx`, `orders/new/page.tsx`, `KitchenBoard.tsx` — to use the new `.mutate()` / `.mutateAsync()` interface; login page rewired to call `POST /api/v1/auth/login` with loading state and error toast | Good | ~45min | Converting sync → async mutations changes the caller interface: `fn(args)` becomes `mutation.mutate(args)` or `await mutation.mutateAsync(args)` — update all call sites in the same commit or the build breaks |
| 72 | Claude Code | Frontend | **Cleanup:** deleted `mock.ts` after verifying zero remaining imports (11 import sites updated); fixed `OrderStatus` type to include `'cancelled'` matching backend enum; added `completed` and `cancelled` entries to `statusBadge` Record in dashboard page; production build passes with 0 TypeScript errors | Good | ~15min | Always run `npm run build` (not just `npm run dev`) after large refactors — TypeScript strict mode catches implicit `any` indexing that the dev server ignores |

### Session — May 6, 2026 (FE §3.2 audit + FE-2 / FE-9 follow-up; Claude Code)

Same-day follow-up to the morning's API wiring session. Audited the frontend against the §3.2 requirements table, verified the state-management acceptance criteria, and closed the two highest-value gaps (FE-2 `useReducer` and FE-9 memoization).

| # | Tool | Area | Purpose | Output Quality | Time Saved | Lessons Learned |
|---|---|---|---|---|---|---|
| 73 | Claude Code | Frontend | **§3.2 requirements audit:** spawned Explore agent to score FE-1 through FE-12; produced a Met / Partial / Missing scorecard with file-and-line evidence (7 fully met, 4 partial, 1 missing — SignalR); separately verified the 5 state-management acceptance criteria (dark-mode persistence, devtools dev-only gating, Zustand cross-component usage, mock-endpoint caching, prod devtool hiding); identified `MultiStepForm` and `KitchenBoard` as the right targets to close FE-2 and FE-9 | Good | ~30min | An audit agent run with concrete file paths and line numbers is the right setup for a §3.2-style checklist — vague prompts produce vague reports; a 600-word target keeps it skim-able for the next reviewer |
| 74 | Claude Code | Frontend | **FE-9 useNow + memoization:** created `hooks/useNow.ts` (interval-driven `Date.now()` subscriber); refactored `KitchenBoard.tsx` — removed parent-level `now` state + effect, switched `byStatus` from a function called 3× per render to a memoized `Record<OrderStatus, Order[]>`, wrapped `onAdvance` in `useCallback` over the stable `setStatus.mutate` reference, wrapped `KitchenOrderCard` in `React.memo` and moved its 30s tick inside via `useNow`; parent now only re-renders when orders change or the 60s clock tick fires, and individual cards re-render in isolation | Good | ~30min | Inline arrow callbacks (`onAdvance={() => onAdvance(order.id, section.next)}`) defeat `React.memo` even when the underlying handler is stable — pass the stable `(orderId, next) => void` to the child and let the child invoke it, so the prop reference is the stable callback itself |
| 75 | Claude Code | Frontend | **FE-2 useReducer in MultiStepForm:** replaced two `useState` hooks (`step`, `photo`) with a `useReducer` driven by a discriminated `WizardAction` union (`next` / `back` / `setPhoto`); reducer enforces step bounds via `Math.min(LAST_STEP, ...)` / `Math.max(0, ...)`; replaced hardcoded `step === 2` checks with a `LAST_STEP = STEPS.length - 1` constant so adding a 4th step won't break submit detection; left `react-hook-form` in charge of field state | Good | ~15min | A reducer should own *flow* state (step, mode, photo), not field state — putting RHF values into a reducer fights RHF's own state machine; the `LAST_STEP` constant is a small but real maintainability win that reviewers always thank you for |
| 76 | Claude Code | DevOps / Frontend | **Dev-server recovery + backend diagnosis:** Next.js 15 surfaced an `InvariantError: Expected clientReferenceManifest` after the branch switch — root cause was a stale `.next/` build cache from the previous branch (an earlier webpack `0.pack.gz` rename error pointed the same way); stopped server, deleted `.next/`, restarted clean. Separately diagnosed why login was broken and navigation felt slow: backend on `:5000` returned `000` (connection refused), so every TanStack Query call hung until retry-and-fail; PR #36 removed `lib/api/mock.ts` and PR #37 wired Keycloak, so previously-created localStorage accounts no longer exist server-side | Good | ~15min | `clientReferenceManifest` invariants in dev are nearly always stale `.next/` after a branch switch — the fix is `rm -rf .next` and restart, not chasing it as a code bug; when a dev server boots clean but pages spin, the next thing to check is whether the API the hooks fetch from is actually listening |

### Session — May 7, 2026 (M3.13 Serilog + correlation IDs; Claude Code)

Closed M3.13 (Person 3 deck) — backend now ships structured JSON logs with per-request correlation IDs to Loki via the existing promtail container. No new transport infrastructure; just app-side wiring against the stack PR #32 already brought up.

| # | Tool | Area | Purpose | Output Quality | Time Saved | Lessons Learned |
|---|---|---|---|---|---|---|
| 77 | Claude Code | Backend / Observability | **M3.13 Serilog + correlation IDs:** added `Serilog.Formatting.Compact` package; wired `Host.UseSerilog()` in `Program.cs` with `ReadFrom.Configuration` + `Enrich.FromLogContext()` + `WriteTo.Console(new CompactJsonFormatter())`; added `Middleware/CorrelationIdMiddleware.cs` that reads or generates `X-Correlation-Id`, echoes it on the response, stashes it in `HttpContext.Items`, and pushes it onto Serilog `LogContext` for the request scope; registered the middleware first in the pipeline followed by `UseSerilogRequestLogging()`; replaced the default `"Logging"` config block in both `appsettings.json` and `appsettings.Development.json` with a `"Serilog"` section (Information / Debug defaults, Microsoft.AspNetCore + EFCore overrides). Backend builds clean (only pre-existing `NU1510` warnings on `Microsoft.Extensions.Http` in Infrastructure) | Good | ~1h | Promtail's docker_sd scraper already ships container stdout to Loki, so a direct `Serilog.Sinks.Grafana.Loki` is redundant — emit CLEF JSON to stdout and let promtail handle transport; this keeps the app decoupled from log storage. `LogContext.PushProperty` inside the middleware's `using` block is the right scope so correlation IDs land on every log emitted during the request, including downstream `ILogger<T>` calls in services without any extra wiring |

---

### Session — Apr 25, 2026 (backend Clean Architecture scaffold; Claude Code) — Olti

Bootstrapped the .NET 10 backend from scratch: Clean Architecture solution structure, initial API setup, and CI ruleset fix.

| # | Tool | Area | Purpose | Output Quality | Time Saved | Lessons Learned |
|---|---|---|---|---|---|---|
| 78 | Claude Code | Backend | **Clean Architecture scaffold:** created 4-layer .NET 10 solution (`DashTab.Domain`, `DashTab.Application`, `DashTab.Infrastructure`, `DashTab.API`) with correct project references (Domain ← Application ← Infrastructure ← API); added mock controller stubs as placeholders to confirm routing worked end-to-end before real entities were built | Good | ~1.5h | Start with a working skeleton (even with mock returns) before building real services — it proves the DI chain and routing are wired before adding complexity |
| 79 | Claude Code | DevOps | **CI ruleset fix:** branch protection ruleset was blocking pushes; fixed the GitHub Actions workflow configuration so CI gates applied correctly without locking out the team | Needed fixes | ~30min | GitHub branch rulesets and classic branch protection rules interact — confirm which one is active before adding status check requirements |

---

### Session — May 4–5, 2026 (backend entities + endpoints + Swagger + exception handler; Claude Code) — Olti

Built the full backend data model and all four endpoint groups, wired Swagger with bearer auth support, and added global exception handling.

| # | Tool | Area | Purpose | Output Quality | Time Saved | Lessons Learned |
|---|---|---|---|---|---|---|
| 80 | Claude Code | Backend | **EF Core entities + DbContext:** created all domain entities (`Order`, `OrderItem`, `MenuItem`, `MenuCategory`, `User`) with base `AuditableEntity` (`CreatedAt`, `UpdatedAt`, `CreatedById`); configured relationships, snake_case naming convention, and `DbContext` with all `DbSet` properties; added EF Core packages and Npgsql provider | Good | ~1.5h | Define `BaseEntity` and `AuditableEntity` first — every other entity inherits them and it prevents duplication of `Id`, `CreatedAt`, `UpdatedAt` across 5+ classes |
| 81 | Claude Code | Backend | **CRUD endpoints (Menu, Orders, Staff, Categories):** implemented service interfaces and concrete services in Infrastructure for all four resource groups; wired controllers with correct HTTP verbs, route templates (`api/v1/...`), and `[Authorize]` / `[Authorize(Roles="Owner,Manager")]` attributes on write endpoints; added `ICurrentUser` service reading `sub` / `email` / `roles` claims from `HttpContext` | Good | ~2h | Use `[Authorize(Roles="...")]` at the action level for write endpoints rather than a policy name — it's readable and maps directly to the Keycloak role names without extra policy registration |
| 82 | Claude Code | Backend | **Swagger + bearer auth:** added `Swashbuckle.AspNetCore` 6.9.0; configured `AddSecurityDefinition("Bearer", ...)` + `AddSecurityRequirement` in `Program.cs`; downgraded from 10.1.7 to 6.9.0 because 10.x requires `Microsoft.OpenApi` 2.x whose `OpenApiSecurityScheme` namespace moved | Needed fixes | ~30min | Always check the Swashbuckle ↔ Microsoft.OpenApi version matrix before installing — major Swashbuckle versions track OpenApi major versions; 6.x uses `Microsoft.OpenApi` 1.x which is stable |
| 83 | Claude Code | Backend | **Global exception handler:** added `DashTabExceptionHandler` implementing `IExceptionHandler`; maps `ValidationException` → 422 with field errors dict, `UnauthorizedAccessException` → 401, `KeyNotFoundException` → 404, unhandled → 500; registered via `AddExceptionHandler` + `UseExceptionHandler` in `Program.cs` | Good | ~30min | Implement `IExceptionHandler` (ASP.NET Core 8+) rather than middleware — it composes cleanly with `IProblemDetailsService` and produces consistent `ProblemDetails` JSON without manual serialization |

---

### Session — May 6, 2026 (backend DB layer — soft delete + audit trail + migrations; Claude Code) — Olti

Added production-quality data layer features: soft delete with global query filter, full audit trail via `SaveChanges` override, and first EF Core migration.

| # | Tool | Area | Purpose | Output Quality | Time Saved | Lessons Learned |
|---|---|---|---|---|---|---|
| 84 | Claude Code | Backend | **Soft delete + global query filter:** added `IsDeleted` flag to `BaseEntity`; overrode `SaveChangesAsync` to intercept `EntityState.Deleted` and flip to `Modified` + set `IsDeleted = true`; added `HasQueryFilter(e => !e.IsDeleted)` on every entity in `OnModelCreating` so deleted rows are invisible to all queries without any call-site changes | Good | ~30min | `HasQueryFilter` is applied globally — use `IgnoreQueryFilters()` only on the specific admin query that needs to see deleted records; forgetting this causes "record not found" on admin restore operations |
| 85 | Claude Code | Backend | **Audit trail via SaveChanges override:** extended the `SaveChangesAsync` override to capture `OldValues` and `NewValues` as JSON for every modified entity and write an `AuditLog` row per change; stored `ChangedById` from `ICurrentUser` so every write is traceable to the authenticated user; added `migrations.md` documenting the migration history and how to apply them | Good | ~45min | Capture `OldValues` from `OriginalValues` before calling `base.SaveChangesAsync` — after the base call the change tracker resets and `OriginalValues` reflects the new state; serialize to JSON inside the override so the audit row is written in the same transaction as the change |

---

### Session — May 6, 2026 (Keycloak JWT auth + RBAC + FluentValidation + rate limiting; Claude Code) — Olti

Full security layer: backend proxies Keycloak token endpoint, JWT bearer validation with role claim mapping, authorization policies, FluentValidation on all DTOs, and rate limiting on auth endpoints.

| # | Tool | Area | Purpose | Output Quality | Time Saved | Lessons Learned |
|---|---|---|---|---|---|---|
| 86 | Claude Code | Backend | **Keycloak JWT bearer auth:** added `Keycloak:Authority` + `Keycloak:Audience` to `appsettings`; configured `AddJwtBearer` with `MapInboundClaims = false` and `RoleClaimType = "roles"` so Keycloak's PascalCase roles claim maps correctly; created `AuthService` proxying Keycloak's ROPC token endpoint via `IHttpClientFactory`; added `AuthController` with `[AllowAnonymous]` login/refresh and `[Authorize]` logout/me endpoints | Needed fixes | ~2h | `MapInboundClaims = false` is essential — without it ASP.NET Core remaps `roles` to a URI-based claim type and `[Authorize(Roles="Owner")]` silently fails with 403 even when the JWT contains the correct role |
| 87 | Claude Code | Backend | **RBAC attributes on all controllers:** added `[Authorize]` at class level on all controllers; added `[Authorize(Roles="Owner,Manager")]` on write actions (POST/PUT/DELETE) for menu items, categories, and users; verified Owner token gets 201, Waiter token gets 403 on the same endpoint via curl | Good | ~30min | Test RBAC with curl rather than Swagger UI — the UI's bearer token input is easy to forget to update between role switches; curl makes the `Authorization` header explicit |
| 88 | Claude Code | Backend | **FluentValidation + rate limiting:** added validators for all request DTOs (`LoginRequest`, `CreateOrderRequest`, `CreateMenuItemRequest`, `UpdateMenuItemRequest`, `CreateCategoryRequest`, `CreateStaffRequest`, `UpdateStaffRequest`); wired `AddFluentValidationAutoValidation` + custom `InvalidModelStateResponseFactory` returning 422 instead of 400; added `AddRateLimiter` with a fixed-window limiter on `/auth/login` (10 req/min) and a global sliding-window limiter (200 req/min) | Good | ~1h | `InvalidModelStateResponseFactory` must be set after `AddFluentValidationAutoValidation` in the service registration order — if set before, the default factory still handles model state errors and returns 400 |

---

### Session — May 9, 2026 (S3-17 frontend Keycloak wiring; Claude Code) — Olti

Wired the frontend to authenticate against real Keycloak via the backend proxy. Replaced all mock auth with real JWT-based login, silent token refresh, and protected routes.

| # | Tool | Area | Purpose | Output Quality | Time Saved | Lessons Learned |
|---|---|---|---|---|---|---|
| 89 | Claude Code | Frontend | **Keycloak auth wiring (S3-17):** created `lib/api/jwt.ts` with `decodeJwt` + `claimsToAuthUser` (base64 JWT decode, `realm_access.roles` extraction, PascalCase→lowercase role normalization); updated `AuthSession` type to match real Keycloak response shape (`accessToken`, `refreshToken`, `expiresIn`); updated `auth.ts` with password param on `login`, added `refreshTokens` and `logoutApi`; extended `useAppStore` with `refreshToken` field, `setTokens` action for silent refresh, updated `setAuth` signature; rewrote `client.ts` 401 handler with refresh-on-401 + retry-once logic using dynamic import to avoid circular dependency; simplified `useAuth` hook to read directly from store instead of calling `/auth/me`; updated login page with real Keycloak demo accounts, controlled password field, click-to-fill both email+password, JWT decode on login response; added route guard in dashboard layout redirecting unauthenticated users to `/login` after store hydration | Good | ~3h | Backend's `.NET` JSON serializer uses camelCase — Keycloak returns snake_case (`access_token`) but the backend re-serializes as camelCase (`accessToken`); always check the backend's naming policy before mapping API response fields. Run the backend locally with `dotnet run --urls http://localhost:5000` for dev — Docker backend uses `keycloak:8080` as the JWT authority but tokens carry `localhost:8080` as issuer, causing 401 on every protected endpoint |

---

### Session — May 8, 2026 (Phase 3A Redis cache-aside + Keycloak auth debugging; Claude Code)

Full session implementing Redis cache-aside on the menu endpoints, then debugging three layered Keycloak authentication issues discovered during live verification. Full write-up in `docs/session-may-08-redis-keycloak.md`.

| # | Tool | Area | Purpose | Output Quality | Time Saved | Lessons Learned |
|---|---|---|---|---|---|---|
| 90 | Claude Code | Backend | **Phase 3A plan:** confirmed Redis was provisioned but unused (`StackExchange.Redis` in csproj, container in docker-compose, zero code); audited what data to cache (menu only — categories + items; orders excluded because stale order state causes real kitchen problems); designed cache-aside with `IDistributedCache` abstraction, 5-min item TTL, 15-min category TTL, fail-open error handling, in-memory fallback for local dev | Good | ~30min | Plan before touching code — knowing that `MenuItemDto.Category` is a denormalized name drove the decision to cascade category-update invalidation to item caches; without that analysis the cache would silently serve stale category names after a rename |
| 91 | Claude Code | Backend | **Phase 3A implementation:** created `ICacheService` (Application layer), `CacheService` (Infrastructure — wraps `IDistributedCache`, all ops try/catch + `ILogger` warning, JSON byte serialization), `CacheKeys` static class; updated `MenuItemService` and `CategoryService` to inject `ICacheService` and apply cache-aside on reads + invalidation on writes; registered `AddStackExchangeRedisCache` / `AddDistributedMemoryCache` fallback in `Program.cs`; build passed clean | Good | ~45min | Serialize to `List<T>` not `IEnumerable<T>` when storing — `IEnumerable` doesn't round-trip through `JsonSerializer.Deserialize`; `IDistributedCache` is already singleton so registering `CacheService` as singleton is correct only as long as it has no scoped dependencies |
| 92 | Claude Code | DevOps | **Keycloak realm missing:** login returned "Invalid credentials" — traced to the `dashtab` realm never having been imported; `--import-realm` only fires on first boot, but Postgres already had Keycloak DB state from a previous run so every restart silently skipped it; fixed by calling the Keycloak admin REST API (`POST /admin/realms`) with the realm-export JSON | Good | ~20min | `--import-realm` is a first-boot-only operation — if the realm DB already exists, Keycloak will never re-import; keep the manual import command in the team runbook and add it to `docs/session-may-08-redis-keycloak.md` for future reference |
| 93 | Claude Code | DevOps / Backend | **JWT issuer mismatch:** after realm import, all requests returned `401 — issuer 'http://localhost:8080/realms/dashtab' is invalid`; root cause: Keycloak stamps tokens with `iss = KC_HOSTNAME = localhost:8080`, but the API's `Authority` was `http://keycloak:8080/realms/dashtab`; fixed by adding `Keycloak__ValidIssuer: http://localhost:8080/realms/dashtab` to the backend env in docker-compose and wiring `opts.TokenValidationParameters.ValidIssuer` in `Program.cs` | Good | ~20min | Always decouple the OIDC discovery URL (`Authority`) from the expected issuer (`ValidIssuer`) in Docker setups — the internal service hostname and the public hostname Keycloak uses in tokens will differ; reading the `WWW-Authenticate` header is much faster than reading logs for diagnosing 401 root cause |
| 94 | Claude Code | DevOps / Backend | **JWKS unreachable from Docker:** after issuer fix, response changed to `401 — signature key was not found`; root cause: Keycloak always puts `KC_HOSTNAME` (localhost) in the discovery doc's `jwks_uri` regardless of which hostname the request arrived on — so the API (inside Docker) tried to fetch `http://localhost:8080/realms/dashtab/protocol/openid-connect/certs`, but inside a container `localhost` = the container itself, not the host; `KC_HOSTNAME_BACKCHANNEL_DYNAMIC: "true"` had no effect in Keycloak 25; fixed with a `KeycloakBackchannelHandler : HttpClientHandler` set as `opts.BackchannelHttpHandler` that transparently rewrites any outgoing backchannel URL from localhost:8080 to keycloak:8080 | Needed several rounds | ~1h | Keycloak's hostname config flags (`KC_HOSTNAME_STRICT_BACKCHANNEL`, `KC_HOSTNAME_BACKCHANNEL_DYNAMIC`) are unreliable in Keycloak 25 — a `BackchannelHttpHandler` URL rewrite is a simpler, version-agnostic fix that requires zero Keycloak reconfiguration; verify JWKS reachability by running `docker run --rm --network <net> alpine/curl curl <jwks_uri>` before debugging the app |

---

### Session — May 11, 2026 (S3-25/S3-26 Hangfire + transactional email; Claude Code) — Olti

Added background job processing and transactional email confirmation on order creation. Verified end-to-end: order created → Hangfire job succeeds → email appears in MailHog.

| # | Tool | Area | Purpose | Output Quality | Time Saved | Lessons Learned |
|---|---|---|---|---|---|---|
| 95 | Claude Code | Backend | **Hangfire + MailKit setup (S3-25):** added `MailKit` 4.8.0 to Infrastructure; added `Smtp` config section to `appsettings.Development.json`; created `IEmailService` interface in Application/Interfaces; created `EmailService` in Infrastructure using `MailKit.Net.Smtp.SmtpClient` pointed at MailHog (localhost:1025, no auth); created `OrderEmailJob` in `Infrastructure/Services/Jobs/` with `[AutomaticRetry(Attempts=3)]` decoration; wired `AddHangfire` + `AddHangfireServer` with Postgres storage (`Hangfire.PostgreSql`) in `Program.cs`; added `OwnerOnlyDashboardFilter` with loopback IP bypass for dev; registered `IEmailService` and `OrderEmailJob` as Scoped | Good | ~2h | `HasDefaultValue(false)` in EF Core `OnModelCreating` tells EF Core the DB column has a default — so EF Core omits the column from INSERT when value is `false`, causing a NOT NULL violation if the DB column has no actual `DEFAULT false`. Fix: run `ALTER TABLE ... ALTER COLUMN is_deleted SET DEFAULT false` on all affected tables to align the DB with the EF Core model config |
| 96 | Claude Code | Backend | **Hangfire enqueue on order creation (S3-26):** injected `IBackgroundJobClient` into `OrderService` primary constructor; added `backgroundJobs.Enqueue<OrderEmailJob>(j => j.SendOrderConfirmation(order.Id))` after `SaveChangesAsync` in `CreateAsync`; smoke tested via Swagger — order 201 returned immediately, Hangfire dashboard showed job Succeeded, MailHog showed HTML email with order number, table, items, quantities, unit prices, and total | Good | ~30min | Enqueue after `SaveChangesAsync`, never before — the Hangfire worker can start the job immediately and will load the order from Postgres; if the order isn't committed yet it returns null and the email is silently skipped with no retry |

---

### Session — May 10, 2026 (M3.4 SQL optimization — indexes + EXPLAIN ANALYZE doc; Claude Code)

Closed M3.4 — identified the three missing indexes on high-traffic columns, added them via EF Core migration, ran `EXPLAIN ANALYZE` against a seeded dev database, and produced `docs/sql-optimization.md` covering all three queries with before/after plans and N+1 elimination proof.

| # | Tool | Area | Purpose | Output Quality | Time Saved | Lessons Learned |
|---|---|---|---|---|---|---|
| 97 | Claude Code | Backend / Database | **SQL optimization indexes (migration):** audited existing indexes against the three busiest query patterns; added `HasIndex(o => o.Status)` and `HasIndex(o => o.PlacedAt)` on `Order` and `HasIndex(m => m.IsAvailable)` on `MenuItem` in `DashTabDbContext.OnModelCreating`; generated EF Core migration `20260510203329_SqlOptimizationIndexes` via `dotnet ef migrations add`; applied to the running Postgres container with `dotnet ef database update` | Good | ~30min | `dotnet ef migrations add` does not need a live DB — it only needs the project to compile and a design-time factory; only `database update` needs a real connection. The `AppDbContextFactory` hardcoded password must match the running container's `POSTGRES_PASSWORD` or the update step fails with a 28P01 auth error |
| 98 | Claude Code | Backend / Database | **EXPLAIN ANALYZE + documentation:** seeded 200 orders, 30 menu items, and 4 categories into the dev database; ran `EXPLAIN ANALYZE` with `BUFFERS` on all three target queries (orders by status + ORDER BY placed_at; menu items JOIN category filtered by category_id + is_available; daily COUNT for order-number generation); re-ran with `SET enable_seqscan = off` to capture indexed execution plans since the planner correctly chooses seq scan on small dev tables; produced `docs/sql-optimization.md` with real query plans, timing numbers, index inventory, and N+1 elimination proof | Good | ~45min | On a small dev dataset the planner always chooses seq scan — this is correct behaviour, not a missing index. Force index usage with `SET enable_seqscan = off` to show what the plan looks like at production scale; document both plans so graders understand why seq scan appears in the dev run. The fastest gain was the `placed_at` range scan (daily COUNT): 0.705 ms → 0.129 ms (5.5×) even on 200 rows |

---

### Session — May 11, 2026 (MinIO object storage integration; Claude Code) — Enes

End-to-end integration of MinIO for binary file storage. The container had been provisioned in docker-compose since the dev-env hardening session (#65) but nothing was wired to it. This session added the full backend integration with menu-item images as the first consumer. Full write-up in `docs/session-may-11-minio-storage.md`.

| # | Tool | Area | Purpose | Output Quality | Time Saved | Lessons Learned |
|---|---|---|---|---|---|---|
| 99 | Claude Code | Backend | **Storage abstraction + DI wiring:** added `Minio` 6.0.4 to Infrastructure; created `IStorageService` interface in Application (CreatePresignedUploadAsync / GetPublicUrl / ExistsAsync / DeleteAsync) + `PresignedUploadUrl` DTO; created `StorageOptions` config class with split `Endpoint` (server-side, `http://minio:9000`) and `PublicBaseUrl` (browser-facing, `http://localhost:9000`); implemented `MinioStorageService` with internal→public hostname rewrite on presigned URLs; created `StorageBucketBootstrapper` `IHostedService` that idempotently ensures `menu-images` bucket exists and applies a public-read policy on startup, with exponential-backoff retry; wired `IMinioClient` singleton factory + service + hosted service in `Program.cs`; added `Storage` section to `appsettings.Development.json` and `Storage__PublicBaseUrl` env + `minio: condition: service_healthy` dependency to docker-compose backend service | Good | ~1.5h | The internal/public endpoint split is the central design decision for any Dockerized S3 integration — without it, presigned URLs work in `curl` from the API container and silently fail in the browser. String-replacement rewrite of the host in the SDK-generated URL is a 3-line fix that papers over what would otherwise need Keycloak-style backchannel handlers; the presigned signature is computed over object key + headers, not the hostname, so rewriting the host doesn't invalidate the signature. Putting bucket bootstrap inside the app as an `IHostedService` means a fresh `docker compose up` always converges — no separate provisioning step |
| 100 | Claude Code | Backend | **Menu-item image feature + EF migration:** added `string? ImageObjectKey` to `MenuItem` entity; added `ImageUrl` to `MenuItemDto` (defaulted null) + `ImageUploadRequest` / `CommitImageRequest` DTOs; updated `MenuItemMapper` with `[MapperIgnoreSource(ImageObjectKey)]` + `[MapperIgnoreTarget(ImageUrl)]` + symmetric ignores on `ToEntity`/`Update` so Mapperly leaves the field alone; introduced a private `ToDto` wrapper in `MenuItemService` that takes `mapper.ToDto(item) with { ImageUrl = ... }` and computes the URL via `IStorageService.GetPublicUrl` only when `ImageObjectKey` is non-null; replaced every `mapper.ToDto(item)` call site with the new wrapper; added 3 service methods (`RequestImageUploadAsync` / `ConfirmImageAsync` / `RemoveImageAsync`) — confirm path HEADs the object to verify it actually uploaded before persisting the key, and deletes the old object first to prevent storage leaks; added 3 endpoints to `MenuItemsController` gated by `[Authorize(Roles="Owner,Manager")]`; all three write endpoints invalidate `menu:items:all` / `menu:item:{id}` / `menu:items:cat:{categoryId}` Redis keys; ran `dotnet ef migrations add AddMenuItemImage` — added nullable `image_object_key` column on `menu_items` | Good | ~1h | Don't inject infrastructure services (like `IStorageService`) into a stateless Mapperly mapper — keep the mapper pure and add a thin service wrapper that handles computed fields. The `[MapperIgnore*]` attributes form the contract between mapper and service; without them Mapperly tries to map `ImageObjectKey → ImageUrl` and either errors or silently leaks the raw key. Generating the migration with `dotnet ef migrations add` also emitted unrelated `AlterColumn is_deleted` calls on every table — earlier `HasDefaultValue(false)` model changes hadn't been captured in a migration; harmless no-ops but worth recognizing as model-snapshot drift, not a bug in this migration |
| 101 | Claude Code | Docs | **Session writeup + ai-log update:** created `docs/session-may-11-minio-storage.md` covering what MinIO is in this project's context, where every piece lives (file paths per layer), step-by-step request flows for upload (presigned URL → direct PUT to MinIO → confirm), display (`<img src>` directly hits public bucket), delete, and the bootstrap path; documented the two-URL split and why presigned-URL host rewriting is needed in Docker; documented the mapper-wrapper pattern. Appended this session block + bumped totals in `ai-logs.md` | Good | ~20min | When the session involves multiple subtle design decisions (internal/public split, mapper wrapper, presigned-vs-proxied), a dedicated session MD pays for itself the first time another teammate asks "why two URLs?" — answering it once in writing beats answering it three times in chat. Follow the existing `session-may-08-*.md` template so the docs directory stays scannable |

---

### Session — May 12, 2026 (MinIO hardening + backend hygiene; Claude Code) — Enes

Follow-up to the May 11 MinIO integration. Reviewed the as-built code, found three real security gaps (dead `contentType` param, no extension validation, no size cap) plus six minor issues; fixed everything in three phases. Also restructured the backend test layout to fix a CI path-filter bug, split the bundled migration, and bumped two vulnerable dependencies. Full write-up in `docs/session-may-12-minio-hardening.md`.

| # | Tool | Area | Purpose | Output Quality | Time Saved | Lessons Learned |
|---|---|---|---|---|---|---|
| 102 | Claude Code | Backend / Security | **Image-upload security pass:** bound `Content-Type` into the presigned PUT via `WithHeaders` so MinIO rejects mismatched uploads; added `ImagePolicy` (allowed extensions + 5 MB cap + content-type map) + `ImageUploadRequestValidator` (FluentValidation, picked up by the existing auto-registration so unsupported extensions return 422 at the API boundary); replaced `ExistsAsync` with `StatAsync` returning `StoredObjectInfo(Size, ContentType)` so confirm can enforce size + content-type in one round-trip and delete the orphan + throw `InvalidOperationException` (→ 400) on violation; added defensive `ResolveContentType` guard in the service so a missing validator can't bypass; added object-key prefix check in `ConfirmImageAsync` (`menu-items/{id}/`) returning null (→ 404) to prevent cross-item key reuse by authenticated callers | Good | ~1h | A signed presigned URL only enforces what you actually sign into it — accepting a `contentType` parameter and never wiring it through `WithHeaders` is silently wrong. Defensive validation at multiple layers is cheap: the same `ImagePolicy` constant powers both the validator (422 at the API edge) and the service guard (400 if anything got past). Return 404 not 400 for prefix mismatches so an attacker can't probe whether a foreign object exists. |
| 103 | Claude Code | Backend / Hygiene | **Layout + dependency cleanup:** centralized `"menu-images"` to `Application/Storage/StorageBuckets.MenuImages` (referenced by service + bootstrapper); refactored `StorageBucketBootstrapper.Buckets` from `string[]` to `BucketSpec(Name, PublicRead)[]` so future private buckets don't inherit public-read by default; pinned `Microsoft.EntityFrameworkCore.Relational 9.0.4` in Infrastructure to silence MSB3277 (Hangfire.PostgreSql transitively pulled 9.0.1); bumped MailKit/MimeKit `4.8.0 → 4.16.0` clearing two NU1902 advisories; soft-delete (`MenuItemService.DeleteAsync`) now deletes the MinIO object and clears `ImageObjectKey` before flipping `IsDeleted` so public-read images don't outlive the menu item; moved backend tests from `tests/backend/DashTab.UnitTests/` → `src/backend/tests/DashTab.UnitTests/` via `git mv` so the backend-ci `paths: 'src/backend/**'` filter actually covers them (previously a test-only PR didn't trigger CI); updated csproj `ProjectReference` paths (one level shallower) and the `.sln` path; split `20260511155639_AddMenuItemImage` into `20260512175529_AlignIsDeletedDefaults` (the 5 `AlterColumn` calls — real schema drift from `OnModelCreating` declaring `HasDefaultValue(false)` that `InitialCreate` never persisted) + `20260512175625_AddMenuItemImage` (only `AddColumn<string>("image_object_key")`) using a `[NotMapped]` toggle to surgically split via two `dotnet ef migrations add` calls | Good | ~45min | Path filters on CI workflows lie if the layout drifts — `paths: 'src/backend/**'` looked right but didn't match `tests/backend/**`; the cheap fix was moving the tests under the prefix, not expanding the filter. `[NotMapped]` is the cleanest way to surgically split a bundled migration: mark the new property, generate the "everything else" migration, unmark, generate the property's migration. Build warnings dropped 15 → 4 — MailKit 4.16's tighter NRT annotations surfaced 2 latent CS8604 in `EmailService.cs` (`config["Smtp:From"]` returns `string?`) which is a real null-deref risk worth flagging for follow-up. |
| 104 | Claude Code | Backend / Tests | **Image-flow test suite + restructure:** added `Microsoft.EntityFrameworkCore.InMemory 9.0.4` + `ProjectReference` to `DashTab.Infrastructure` in the test csproj; wrote `MenuItemServiceImageTests.cs` with 19 cases covering `RequestImageUploadAsync` (item-not-found, 3 unsupported extensions, 4 valid extensions verifying scoped object key + bound content type), `ConfirmImageAsync` (cross-item key rejected with storage untouched, item-not-found, object missing, oversized → delete + throw + not persisted, non-image content-type → delete + throw, happy path → key persisted + old image deleted), `RemoveImageAsync` (3 paths), and `DeleteAsync` (with/without image); used hand-rolled `FakeStorageService` (records `Deletes`, scriptable `StatHandler`, captures `LastContentType`) + `NoopCacheService` instead of pulling in Moq/NSubstitute; backend test count went from 27 → 46 (all green) — the new tests now run on every CI build because the relocated test project sits under the backend-ci path filter | Good | ~45min | Hand-rolled fakes are the right tool for service-level tests when the interface is small — 30 lines per fake, zero package dependencies, reads as plainly as English. EF Core InMemory provider plus a fresh DbContext per test (`UseInMemoryDatabase(Guid.NewGuid().ToString())`) gives clean isolation without TestContainers overhead. The structural fix (relocating tests) and the test additions reinforce each other: more tests are only useful if CI actually runs them. |
